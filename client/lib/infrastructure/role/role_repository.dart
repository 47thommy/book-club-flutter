import 'package:client/domain/role/role.dart';

class RoleRepository {
  createRole(Role role, int groupId, String token) =>
      throw UnimplementedError();
  updateRole(Role role, String token) => throw UnimplementedError();
}
